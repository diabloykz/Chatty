<div class="ui small inverted menu ">
  <div class="ui container">
    <%= link_to 'Chatroom', root_path , class: "red #{active_page?(root_path)} item" %>
    <a class="red item">
      Messages
    </a>
    <div class="right menu">
      <div class="ui dropdown item">
        Compte <i class="dropdown icon"></i>
        <div class="menu">
          <% if logged_in?  %>
            <%= link_to "Se déconnecter" , logout_path, method: :delete, class: "item" %>
          <% else %>
            <%= link_to 'Se connecter', login_path, class: "item" %>
          <% end %>
        </div>
      </div>
      <% if !logged_in? %>
        <div class="item">
            <%= link_to "Se connecter", login_path, class: "ui red primary button" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
